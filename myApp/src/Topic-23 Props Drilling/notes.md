## What is props drilling

Props drilling is the process of passing data from a parent component down to a deeply nested child component through intermediate components that don't actually need that data — they just pass it along.