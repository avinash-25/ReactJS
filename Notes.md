- For install a react **create react app** --> This is depricated

## Framework  

- Framework is a set of libraries.

## librarary

- libraries is set of prefefiened code.
- libraries have only relevent modules and code.
- using react we can create only ui btt using next.js we can create both in same file.

## CSR 

- Rendering happend at the client side.
- It is slow

## SSR

- rendering happend at the server side.
- This is fast.

## Single Page Application (SPA)

- In SPA only one req goes at a time and one res getback and if i send another req then prev response saved to the cache memory.
- Page not to be reloaded when new request send to the server.
- Todays, mostly websites uses SPA.
- usign react we can only create single page ui. but if we have to goes about page, contact page and i have to navigae then other library we have to use.


## Multi page Application.

- When new request goes to the server then previous request goes with that and whole page reload so this is slow.

## XML

- each element or expressions have a common parent then the render on the ui. other wise we will got an error.
- this is case sensitive

## componenet based architecture.

- First letter of a component must be a capital.
- There is a two types of component
    - functional based component
    - class based component

# Day - 3

- SPA
- React is decleartive means use the predefiende code.
- component based architecture (Dividing the web page into diff diff web page)
   1. class based
      - js classes used
      - nowdays this is not so much used
      - statefull
      - No hooks
      - live cycle methods
      - this keyword is avaliable
      - render() method is avaliable
  

   2. function based
      - used js functions
      - stateless
      - for make statufull new fetures introduces **hooks**
      - No life cycle methods
      - No this keyword
      - No reder() method
  

- virtual DOM
 
