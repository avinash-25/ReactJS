## Module

### Named export

- Named export can be multiple in one file
- exported name and imported name must be same
- we can also import as **exported_name as alias_name** also.


### Default export

- Default export can be only one in a file.
- can be import by any name